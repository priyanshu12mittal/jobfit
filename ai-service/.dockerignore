# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Virtual environment — we install deps fresh inside the container
.venv/
venv/
env/

# Local .env with secrets — NEVER copy into an image
.env

# Editor and OS noise
.DS_Store
.idea/
*.egg-info/
