server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobfit
    username: ${DB_USERNAME:priyanshu}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

ai-service:
  base-url: ${AI_SERVICE_URL:http://localhost:8000}
